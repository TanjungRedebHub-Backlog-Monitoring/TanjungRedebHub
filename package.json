{
  "name": "spx-backlog-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "SPX Backlog Monitoring Dashboard - static HTML served via serve",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
